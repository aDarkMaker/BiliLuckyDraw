<div align="center">
  <img src="../../img/logo.png" width="160" alt="BiliLuckyDraw logo" />

  # BiliLuckyDraw

  A Bilibili live-stream danmaku lucky-draw desktop app built with Wails v3 + Go + React + TypeScript

  ![Wails](https://img.shields.io/badge/Wails-v3-red?style=for-the-badge)
  ![Go](https://img.shields.io/badge/Go-1.25+-00ADD8?style=for-the-badge&logo=go&logoColor=white)
  ![React](https://img.shields.io/badge/React-18-61DAFB?style=for-the-badge&logo=react&logoColor=white)
  ![TypeScript](https://img.shields.io/badge/TypeScript-5-3178C6?style=for-the-badge&logo=typescript&logoColor=white)
  ![License](https://img.shields.io/badge/License-MIT-blue?style=for-the-badge)

  **English** · [简体中文](./README.zh-CN.md)
</div>

---

## Introduction

BiliLuckyDraw is a desktop app for running lucky draws on Bilibili live streams. Once started, it monitors the live-stream chat (danmaku) in real time, collects participants by keyword, and randomly picks winners. It supports multi-account profiles, QR-code / cookie login, and three themes (light / dark / spring festival).

## Features

- **Live-stream draw**: monitor danmaku in real time, collect participants by keyword, draw randomly
- **Multiple login methods**: cookie login, Bilibili-app QR-code login
- **Multi-account profiles**: multiple draw configs (keyword / winner count / watched rooms), one-click switch
- **Theme system**: light (default, follows system), dark, spring festival — switchable at runtime, persisted
- **Customizable**: custom keywords, watched rooms, background image

## Screenshots

<div align="center">
  <table>
    <tr>
      <td align="center"><img src="../../img/home.png" width="400" alt="Home / draw"/><br/><sub>Home / draw</sub></td>
      <td align="center"><img src="../../img/login.png" width="400" alt="Login"/><br/><sub>Login</sub></td>
    </tr>
    <tr>
      <td align="center"><img src="../../img/prcode.png" width="400" alt="QR login"/><br/><sub>QR login</sub></td>
      <td align="center"><img src="../../img/settings.png" width="400" alt="Settings"/><br/><sub>Settings</sub></td>
    </tr>
  </table>
</div>

## Quick Start

1. **Download**: grab the installer for your platform from [Releases](https://github.com/aDarkMaker/BiliLuckyDraw/releases)
2. **Login**: via cookie or QR code
3. **Configure rooms**: add the live-stream rooms you want to monitor in Settings
4. **Start the draw**: click start — the app monitors danmaku and collects participants in real time

## Download

- **[GitHub Releases](https://github.com/aDarkMaker/BiliLuckyDraw/releases)**

## Tech Stack

| Layer | Stack |
| --- | --- |
| Backend | Wails v3 · Go 1.25 |
| Frontend | React 18 · TypeScript 5 · Vite · Bun |
| Transport | WebSocket · Gorilla |

## Project Structure

```
luckydraw/
  main.go                        # Wails v3 entry (embeds frontend dist)
  Taskfile.yml                   # build workflow (dev/build/package/generate)
  build/                         # per-platform build configs & Taskfiles
  internal/
    app/                         # thin Wails service layer (only pkg importing wails/v3), delegates to service
    service/                     # pure business logic (auth/live/profile), Wails-free
    domain/                      # interfaces + DTOs
    event/                       # Emitter interface (decouples service from Wails)
    config/ bili/ live/ login/   # config, Bilibili API, live-stream danmaku protocol, QR login
  frontend/
    src/
      themes/                    # theme system: light / dark / spring-festival
      styles/ components/ hooks/ # structural tokens, component styles, React hooks
    bindings/                    # generated by wails3 generate bindings (do not edit)
```

## Development & Build

### Prerequisites

- Go 1.25+
- Bun (or npm / pnpm / yarn)
- Wails v3 CLI: `go install github.com/wailsapp/wails/v3/cmd/wails3@latest`
- Task: `go install github.com/go-task/task/v3/cmd/task@latest`

### Local Development

```bash
git clone https://github.com/aDarkMaker/BiliLuckyDraw.git
cd BiliLuckyDraw/luckydraw
task common:install:frontend:deps   # install frontend deps (bun)
task dev                            # dev mode (hot reload)
```

### Build & Package

```bash
task build                       # build an executable for the current platform
task darwin:package              # package a macOS app
task windows:package             # package a Windows app
task common:generate:bindings    # regenerate frontend bindings after changing backend methods
```

> See `luckydraw/Taskfile.yml` and `luckydraw/build/` for details.

## Notes

- Frontend bindings live in `frontend/bindings/`, generated by `wails3 generate bindings` — **do not hand-edit**; regenerate after changing backend methods
- Theming uses CSS variables scoped by the `theme-light` / `theme-dark` / `theme-spring-festival` class; all colors go through tokens
- Backend layering: `app` (Wails boundary) → `service` (pure logic) → `domain` (interfaces) / `event` (event decoupling); `app` is the only package importing wails/v3

## License

[MIT License](../../LICENSE) © 2025 aDarkMaker
